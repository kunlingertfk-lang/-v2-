# 颜色识别算子实施计划

## Summary

- 在项目根目录新建 `AGENTS.md`，写入“颜色识别”算子的 UI 与算法实施计划。
- 本阶段只添加规划文档，不修改现有源码、UI 文件、工程文件或算法代码。
- 后续真正执行时，必须严格按 `AGENTS.md` 中的计划实施。

## 约束

- 当前阶段不得修改现有代码。
- 后续实现前必须再次获得用户确认。
- 实现时只允许做颜色识别算子所需的最小接入，不得重构无关代码。

## UI 方案

- 参考用户提供截图，新增 `颜色识别` 配置界面。
- 左侧包含：`模板训练`、`检测区域`、`结果判断` 三个配置区。
- 顶部保留 `基础 / 全部` 分段切换。
- 右侧为基准图/当前图像预览区，支持 ROI 显示、OK/NG 状态、算法耗时和工具耗时。
- 第一版检测区域只支持矩形 ROI；自由区域、圆形区域先保留按钮或提示暂未接入。

## 算法方案

- 使用 OpenCV 4.8.0，不引入新依赖。
- 使用 HSV 颜色空间做目标颜色识别。
- 流程：ROI 裁剪 -> BGR 转 HSV -> 目标颜色阈值分割 -> 形态学去噪 -> 轮廓统计 -> 计算目标色占比。
- 默认判断：目标色覆盖率达到最低分数/阈值则 OK，否则 NG。

## 后续代码接入点记录

- `ToolTypes.h` 增加颜色识别类型。
- `ToolLibraryDialog` 把“颜色识别”加入识别工具。
- 新增 `ColorRecognitionDialog` 配置窗口。
- 新增 `ColorRecognitionAdapter` 和 OpenCV runner。
- `MainWindow` 注册 adapter。
- `qt_ui_test.pro` 加入新增源码、头文件、UI 文件。

## Test Plan

### 文档阶段

- 确认根目录生成 `AGENTS.md`。
- 确认文档只包含计划，不修改源码。

### 后续实现阶段

- qmake + make 编译通过。
- 工具库能看到“颜色识别”。
- 能新建、保存、编辑颜色识别工具。
- 单次运行能输出 OK/NG、覆盖率、ROI 叠加框。
- 空图像、无效 ROI、灰度图输入时不崩溃。

## Assumptions

- 当前阶段只写计划文档。
- 真正实现颜色识别算子需要后续单独确认。
- 后续实现时可最小化修改必要接入点，但不得改动无关现有算子行为。
